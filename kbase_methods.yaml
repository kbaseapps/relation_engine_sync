update_provenance:
  title: Update Provenance
  description: >-
    Update the ArangoDB workspace provenance graph from a user ID.
    This will iterate over all workspace and all objects for the user and sync
    data under the `wsprov` arango namespace.
  required_params: [uid]
  params:
    uid:
      title: User ID
      type: string
